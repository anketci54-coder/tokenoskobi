# TOKENOSKOBI NEXT CHAT HANDOFF

Start in new window:

```bash
cd /root/tokenoskobi_clean_v1 || exit 1
tk ai
tk sync
git status --short
```

Source of truth: repository and server state.

Do not open a new ERA unless explicitly requested.

Prefer GitHub inspection before asking for terminal output.

Current focus: root cleanup, archive normalization, registry reduction, plugin/workspace activation, boot and handoff simplification.
